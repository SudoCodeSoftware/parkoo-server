#!/usr/bin/python
from MySQLdb import *

def resetPassword(connection):
    #Reset the password in case the user forgets it
    
def changeNumber(connection):
    #Change the phone number registered to the account
